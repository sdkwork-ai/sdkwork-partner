# Adaptive Web roots for sdkwork-partner (copy into runtime config when in-process).
# Docker entrypoint generates /etc/sdkwork/webserver/module-app-roots/sdkwork-partner.toml

[app_roots]
tablet_surface = "pc"

[app_roots.pc_static_by_environment]
development = "apps/sdkwork-partner-pc/dist/dev"
test = "apps/sdkwork-partner-pc/dist/test"
staging = "apps/sdkwork-partner-pc/dist/staging"
production = "apps/sdkwork-partner-pc/dist/prod"

[app_roots.static_fallback_by_environment]
development = "deployments/webserver/static"
test = "deployments/webserver/static"
staging = "deployments/webserver/static"
production = "deployments/webserver/static"
