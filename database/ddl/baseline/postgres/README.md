# PostgreSQL baseline DDL

Optional PostgreSQL authoritative-server baseline snapshots when `baselineStrategy` is not `migrations-only`.

Do not copy or mechanically translate this baseline into SQLite. Client-local SQLite uses `templates/database-client-local/` and owns a separate contract.
